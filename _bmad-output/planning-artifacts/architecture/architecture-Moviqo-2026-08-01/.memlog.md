---
scope: Moviqo MVP project architecture
purpose: Build substrate for implementation teams and coding agents
altitude: feature
updated: 2026-08-01T21:43
---

- (event) Fast-path architecture run started; sole deliverable is a feature-altitude build substrate for implementation teams and coding agents.
- (constraint) The PRD and its memlog are authoritative; the finalized UX package is downstream input, and conflicts resolve in favor of the PRD.
- (constraint) The repository is greenfield: Moviqo.Front, Moviqo.Back, Moviqo.AI, and Moviqo.Infraestructure are empty placeholders with no manifests or application code to ratify.
- (constraint) MVP is a bilingual public-beta multi-tenant SaaS sized for 20 active Organizations and 50 concurrent users, with 2-second p95 core operations excluding files and email.
- (constraint) One account belongs to exactly one Organization in MVP; every tenant-owned record and protected operation carries immutable Organization context and denies mismatches without leaking resource existence.
- (constraint) Published workflow versions and completed task/history evidence are immutable; active instances use the newest compatible publication for future routing, while each task completion uses exactly one version.
- (constraint) Core business commands are idempotent and all-or-nothing; email or other asynchronous delivery failure never reverses committed business state.
- (constraint) Private files require server authorization, malware inspection, short-lived read access, immediate application-level revocation on removal, and audited cleanup.
- (constraint) Production database and attachments require daily encrypted exports outside the primary failure boundary, 7 daily and 4 weekly retention, 24-hour RPO/RTO, alerts, and tested isolated restores before real-data onboarding, quarterly, and after material storage changes.
- (constraint) Development and UAT contain no customer data; Gate 1 uses persistent synthetic data and server-enforced tenant isolation, while Gate 2 adds production safeguards and retained release evidence.
- (version) .NET 10.0.10 LTS verified against the official .NET support policy on 2026-08-01; supported through November 2028.
- (version) EF Core 10 LTS verified against Microsoft documentation on 2026-08-01.
- (version) React 19.2.7 verified against the official React versions page on 2026-08-01.
- (version) Vite 8.2 verified as the regular-patch line in official Vite release documentation on 2026-08-01.
- (version) Node.js 24.18.0 LTS verified against the official Node.js release table on 2026-08-01.
- (version) TypeScript 6.0 verified against the official release notes updated 2026-07-20.
- (version) PostgreSQL 18.4 verified against the official PostgreSQL version policy on 2026-08-01; major 18 is supported through November 2030.
- (version) React Flow @xyflow/react 12.11.2 verified against the official project site on 2026-08-01.
- (version) ClamAV 1.5.3 verified as the latest stable release on the official download page on 2026-08-01.
- (assumption) [ASSUMPTION] Adopt a modular monolith with hexagonal module internals and a deterministic workflow interpreter; this prevents premature distributed-system complexity while enforcing module dependency direction.
- (assumption) [ASSUMPTION] Seed Moviqo.Back from the ASP.NET Core 10 webapi template and Moviqo.Front from the official Vite React TypeScript template; retain separate static frontend and API deployment units.
- (assumption) [ASSUMPTION] Backend modules are Organizations, WorkflowDesign, WorkflowRuntime, Files, Messaging, and Governance; modules own their data and expose only application contracts or integration events.
- (assumption) [ASSUMPTION] Use one PostgreSQL database: relational tables for identities, tenancy, metadata, tasks, lifecycle, audit, outbox, and file records; versioned JSONB documents for dynamic workflow definitions, rule ASTs, forms, and process-field values.
- (assumption) [ASSUMPTION] Enforce tenant isolation twice: explicit TenantContext and authorization in every application use case plus PostgreSQL row-level security and Organization-scoped foreign keys for tenant tables.
- (assumption) [ASSUMPTION] Every state-changing command executes through one backend application use case and one database transaction that writes business state, immutable audit evidence, idempotency result, and outbox messages together.
- (assumption) [ASSUMPTION] Serialize workflow publication and task writes on the WorkflowDefinition row; use optimistic revisions for drafts and submissions, and record the exact published version used by each task occurrence.
- (assumption) [ASSUMPTION] Represent conditions, calculations, validations, and routing as a versioned typed AST with no executable scripts; backend evaluation is authoritative and frontend preview calls the same backend interpreter.
- (assumption) [ASSUMPTION] Use same-origin secure HttpOnly cookie authentication with ASP.NET Core Identity, anti-forgery protection on unsafe requests, and no browser-stored bearer tokens.
- (assumption) [ASSUMPTION] Use REST/JSON with OpenAPI as the API contract, generated TypeScript clients, RFC 9457 Problem Details, UUIDv7 identifiers, UTC instants, IANA time zones, ISO dates, and invariant decimal storage.
- (assumption) [ASSUMPTION] Upload files into private quarantine through authorized short-lived upload grants, scan with ClamAV, then promote clean objects; task completion cannot consume pending, rejected, or infected attachments.
- (assumption) [ASSUMPTION] Run email, file cleanup, exports, lifecycle enforcement, and outbox delivery in a separately scaled worker from the same backend codebase using PostgreSQL leases; add no broker or Redis for beta.
- (assumption) [ASSUMPTION] Deploy a static SPA at an edge/CDN, containerized API and worker, managed PostgreSQL, private object storage, managed email, and isolated backup storage; primary cloud provider remains a review choice, with Azure as the current lean.
- (assumption) [ASSUMPTION] Use OpenTelemetry-compatible structured telemetry with correlation IDs and redaction, GitHub Actions gates, container images, and separate local, CI, UAT, and production configurations.
- (direction) Keep Moviqo.AI outside the MVP runtime and dependency graph; AI-assisted product capabilities require a later requirements and threat-model decision.
- (question) Review load-bearing assumptions: .NET/React modular monolith versus Next.js or NestJS alternatives, and Azure-first deployment versus a lower-cost provider-neutral beta deployment.
- (version) Npgsql.EntityFrameworkCore.PostgreSQL 10.0.2 verified from the official Npgsql repository release on 2026-08-01.
- (version) Azure Database for PostgreSQL Flexible Server officially supports PostgreSQL 18.4 as of 2026-07-08.
- (direction) Version verification sources: https://dotnet.microsoft.com/en-us/platform/support/policy ; https://react.dev/versions ; https://vite.dev/releases ; https://nodejs.org/en/about/previous-releases ; https://www.typescriptlang.org/docs/handbook/release-notes/typescript-6-0.html ; https://www.postgresql.org/support/versioning/ ; https://learn.microsoft.com/en-us/azure/postgresql/configure-maintain/concepts-supported-versions ; https://www.npgsql.org/doc/release-notes/10.0.html ; https://reactflow.dev/whats-new ; https://www.clamav.net/download.html
- (event) Fast-path spine distilled; mechanical lint passed with zero findings; awaiting user review of assumptions.
- (decision) [ADOPTED by user] Use a Python modular-monolith backend with React; learning and entering the Python ecosystem is an explicit architectural objective.
- (decision) The Python decision supersedes the earlier .NET, ASP.NET Core, EF Core, and Npgsql stack assumptions; stable AD identifiers remain unchanged while their rules are amended.
- (assumption) [ASSUMPTION] Use Python 3.14.6 with Django 5.2.15 LTS and Django REST Framework 3.17.1; Django is preferred over FastAPI because Moviqo benefits from mature sessions, CSRF, ORM/migrations, transactions, security defaults, and admin capabilities.
- (version) Python 3.14.6 verified from python.org; released 2026-06-10.
- (version) Django 5.2.15 LTS verified from the official Django download/support table; Python 3.14 support is official from Django 5.2.8 and extended support runs through April 2028.
- (version) Django REST Framework 3.17.1 verified from the official encode release repository; it supports Python 3.14 and Django 5.2/6.0.
- (version) Psycopg 3.3.4 verified from the official Psycopg download page; new projects should use Psycopg 3.
- (version) drf-spectacular 0.30.0 verified from its published package metadata; it supports Django 5.2 and DRF 3.17 and generates OpenAPI 3.x.
- (assumption) [ASSUMPTION] Recommended zero-cost beta composition: Cloudflare Pages for the React SPA, Google Cloud Run service for Django API, Cloud Run Jobs for outbox/lifecycle/scan/backup work, Supabase Free PostgreSQL, Cloudflare R2 private attachments, Backblaze B2 independent backups, and Resend Free email.
- (constraint) Free tiers cannot guarantee all approved maximum product quotas: Supabase Free is 500 MB database and pauses after one inactive week; Resend Free is 3,000 emails/month and 100/day; monitor usage and require a paid upgrade before limits threaten active Organizations or release requirements.
- (direction) Do not use Supabase Auth or direct browser database access; Supabase is PostgreSQL hosting only, while Django owns identity, sessions, authorization, tenant context, and migrations.
- (question) Approve or revise the recommended free beta composition; alternatives are Vercel for frontend only, Render/Koyeb for a simpler but sleeping/limited API, or a paid single-provider deployment when beta usage or security gates require it.
- (version) Supabase platform defaults to PostgreSQL 17 as of 2026-08-01; amend the free-hosting stack seed from PostgreSQL 18.4 to 17.10 while Supabase remains selected.
- (event) Architecture draft amended for Python/Django/React and the recommended zero-cost beta composition; mechanical lint passed with zero findings.
- (decision) [ADOPTED by user] Immediate deployment objective is a company-only, fully deployed end-to-end beta using synthetic data; it must exercise the complete flow before customer onboarding.
- (constraint) The internal E2E phase should stay within no-cost allowances where practical. Paid deployment improvements are evaluated later when real usage, capacity, security, or reliability evidence justifies them; payment is not a prerequisite for company E2E testing.
- (direction) Firebase assessment: use Firebase Hosting for the static React SPA and rewrite /api/** to the Django Cloud Run service. Do not replace Django authentication, authorization, PostgreSQL, or the workflow runtime with Firebase services.
- (assumption) [ASSUMPTION] Amend the zero-cost deployment lean to Firebase Hosting + Cloud Run service/jobs + private Google Cloud Storage in us-east1, Supabase Free PostgreSQL, Backblaze B2 backups, and Resend Free email.
- (constraint) Cloud Run integration and Cloud Storage require a billing-enabled Firebase Blaze project even while usage remains inside no-cost allowances. Configure budget alerts, low maximum instance counts, quotas, and 60/80/90 percent capacity alerts; budget alerts alone are not a spending cap.
- (version) Firebase Hosting currently includes 10 GB static storage and 10 GB monthly transfer at no cost; Hosting can rewrite API paths to a Python Cloud Run container but applies a 60-second request timeout.
- (version) Cloud Storage for Firebase requires Blaze as of 2026-02-03; eligible US-region Google Cloud Storage retains a 5 GB Always Free allowance.
- (decision) [ADOPTED by user] Initial company-only E2E deployment does not require live malware scanning, independent backup/restore automation, or Organization/data lifecycle schedulers; these are deferred until preparation for permitted real customer data.
- (constraint) Internal E2E still preserves the production interfaces and states: synthetic file inspection uses an explicit internal-only adapter, and email dispatch exercises the transactional outbox without coupling email success to business commits.
- (direction) Lifecycle jobs refers to inactivity warnings, Dormant transition, restoration-window expiry, final Organization deletion, export expiration/deletion, stale binary cleanup, and Historical Organization Register expiry. Invitation and lease expiry may be enforced when accessed rather than by a scheduler.
- (constraint) Any environment permitted to hold real customer data must fail startup or release checks if malware inspection, daily independent backups, restore evidence, and required lifecycle enforcement are not configured.
- (decision) [ADOPTED by user] Moviqo uses pragmatic TDD as a binding delivery policy: red-green-refactor for production behavior and defects, with real PostgreSQL integration tests, tenant-isolation tests, contract tests, architecture tests, and Playwright critical journeys; no universal coverage percentage target.
- (event) Architecture approved by user; final reviewer gate started after adding AD-16 Test-first delivery contract.
- (version) pytest 9.1.1 verified from official PyPI release history on 2026-08-01; supports Python 3.14.
- (version) Playwright 1.62 verified from official Playwright release notes on 2026-08-01.
- (decision) [ADOPTED by user] All architecture decisions AD-1 through AD-16 are binding; remaining uncertainty is confined to Deferred.
- (decision) Reviewer gate centralized Internal E2E versus Real-data/Public Beta safeguards and hardened RLS role/context mechanics, outer transaction ownership, idempotency scope, JSONB upcasting, synthetic file inspection, proxy/cache security, and accessibility evidence.
- (event) Reviewer gate passed: deterministic lint reported zero findings; PRD, UX, good-spine, technology-currency, adversarial-divergence, and security/data-integrity reviews passed after autofixes.
- (event) Spine finalized after user approval; status set to final and final mechanical lint passed with zero findings.
